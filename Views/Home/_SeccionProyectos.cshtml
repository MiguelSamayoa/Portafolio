@model List<Proyecto>



@if (Model is not null)
{
    <div>
        <h1 class="text-center mt-5">Proyectos</h1>
        <div class="container justify-content-center">
            <div class="row row-cols-1 row-cols-md-3 g-4">
                <partial name="_IteracionProyectos" model="Model"/>
            </div>

        </div>
    </div>
}